name: Action Test
on:
  pull_request:
  push:

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Send things
        uses: ./
        with:
          token: gQYKfqisxIOjdjTG79kRtKvb1n79Ftem
          project_id: 28szluv
          base_url: https://www.woo.com

